---
---
<section class="security-section">
  <div class="container">
    <h2 class="section-title">Чому це безпечно</h2>
    <div class="security-grid">
      <div class="security-item card-base">
        <div class="security-icon"><img src="/sec-1.svg" alt=""/></div>
        <h3>Повна анонімність</h3>
        <p>Ніхто не дізнається про твій запит</p>
      </div>
      <div class="security-item card-base">
        <div class="security-icon"><img src="/sec-2.svg" alt=""/></div>
        <h3>Тільки людина</h3>
        <p>Жодних ботів — тільки жива, чуйна відповідь</p>
      </div>
      <div class="security-item card-base">
        <div class="security-icon"><img src="/sec-3.svg" alt=""/></div>
        <h3>Прозоро і без впливу</h3>
        <p>Без тиску і маніпуляцій — лише щира відповідь</p>
      </div>
      <div class="security-item card-base">
        <div class="security-icon"><img src="/sec-4.svg" alt=""/></div>
        <h3>Безпечна оплата</h3>
        <p>Без підводних каменів — усе прозоро</p>
      </div>
    </div>
  </div>
</section>

<script is:inline>
document.addEventListener('DOMContentLoaded', function() {
  const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
  };
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.style.opacity = '1';
        entry.target.style.transform = 'translateY(0)';
      }
    });
  }, observerOptions);
  document.querySelectorAll('.security-item').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'opacity 0.6s ease, transform 0.6s ease';
    observer.observe(el);
  });
});
</script>

<style lang="scss">
  .security-icon{
    width: 99px;
    height: 99px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(44, 37, 32, 0.42);
    border: 1px solid rgba(92, 66, 40, 1);
    border-radius: 50%;
    box-shadow: 0px 0px 14px 4px rgba(102, 71, 42, 1) inset;
    margin-bottom: 23px;
  }
  .security-section {
    padding: 100px 0;
  }

  .security-grid {
    display: grid;
    grid-template-columns: repeat(4, 300px);
    gap: 115px;
    margin-top: 60px;
    justify-content: center;
  }

  .security-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 0 10px;
  }

  .security-item h3 {
    font-size: 30px;
    line-height: 37px;
    margin-bottom: 16px;
    color: rgba(254, 209, 167, 1);
    
    font-family: 'Coolvetica', sans-serif;
  }

  .security-item p {
    font-size: 22px;
    color: rgba(202, 195, 188, 1);
    line-height: 32px;
    font-family: 'Montserrat', sans-serif;
  }

  @media (max-width: 1600px) {
    .security-section {
      padding: 100px 0;
    }
    
    .security-grid {
      gap: 40px;
      margin-top: 70px;
    }
    
    .security-item {
      padding: 28px 20px;
    }
    
    .security-item h3 {
      font-size: 28px;
    }
    

  }

  

  @media (max-width: 1200px) {
    .security-section {
      padding: 80px 0;
    }
    
    .security-grid {
      gap: 30px;
      margin-top: 50px;
    }
    
    .security-item {
      padding: 20px 16px;
    }
    
    .security-item h3 {
      font-size: 20px;
    }
    
    .security-item p {
      font-size: 18px;
    }
  }

  @media (max-width: 1024px) {
    .security-section {
      padding: 70px 0;
    }
    
    .security-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 30px;
      margin-top: 40px;
    }
    
    .security-item {
      padding: 24px 20px;
    }
    
    .security-item h3 {
      font-size: 18px;
    }
    
    .security-item p {
      font-size: 15px;
    }
  }



  @media (max-width: 768px) {
    .security-section {
      padding: 50px 0;
    }
    
    .security-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-top: 30px;
    }
    
    .security-item {
      padding: 20px 16px;
    }
    
    .security-item h3 {
      font-size: 14px;
    }
    
    .security-item p {
      font-size: 10px;
    }
  }

  @media (max-width: 480px) {
    .security-section {
      padding: 42px 0;
    }
    .security-icon{
      width: 82px;
      height: 82px;
      margin-bottom: 17px;
    } 
    .section-title {
    margin-bottom: 12px;
}
    .security-icon img{
      width: 39.76px;
    }
    .security-grid {
      grid-template-columns: 1fr;
      gap: 24px;
      margin-top: 32px;
    }
    
    .security-item {
      padding: 0 16px;
    }
    
    .security-item h3 {
      font-size: 26px;
      line-height: 37px;
      margin-bottom: 10px;
    }
    
    .security-item p {
      font-size: 18px;
      line-height: 26px;
    }
  }
</style> 
 